import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1791FF',
      white: '#ffffff',
      black: '#000000',
      orange: '#F59A2A'
    },
    background: {
      default: '#d0d0d0',
    }
  }
})

export default theme
